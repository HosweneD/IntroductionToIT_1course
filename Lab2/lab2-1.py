def greet(name): #приветствие
    hi = f"Здравствуй, {name}"
    return hi

def square(number): #квадрат числа
    return number * number

def max_of_two(x, y): #большее из двух чисел
    return max(x, y)
